<div>
    <% @item.photos do |photo| %>
        <%= cl_image_tag(photo.key) %>
    <% end %>
</div>
<h2>NAME: <%= @item.name %> </h2>
<p>DESCRIPTION: <%= @item.description %> </p>

<%= link_to 'Update', edit_item_path  %>