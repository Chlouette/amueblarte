<h1>This is where the tab bar will appear</h1>

<h1>This is the form to donate / create / decorate a new piece of furniture</h1>

<% if session[:booking_type] == "donation" %>
  <%= simple_form_for([@item, @booking]) do |f| %>
    <%= f.input :start_date %>
    <%= f.submit %>
  <% end %>
<%# OTHER IF CONDITIONS %>
<% end %>
