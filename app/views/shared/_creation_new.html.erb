<div class="container page-container">
  <div class="m-5 text-center">
    <h1>Creation time!</h1>
    <p>When are you able to come to our warehouse?</p>
  </div>
</div>
<div class="container page-container">
  <div class="cards">
    <div class="card-category">
      <ul>
        <li><h4>Nice one, you've decided to decorate this beauty!:</h4></li>
        <br>
        <li>Name: <%= @item.name %></li>
        <li>Description: <%= @item.description %></li>
      </ul>
    </div>
      <% if @item.photos.attached? %>
        <% @item.photos.each do |photo| %>
          <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path photo.key %>')">
          </div>
        <% end %>
      <% else %>
        <div class="card-category" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('https://images.unsplash.com/photo-1564085352725-08da0272627d?ixid=MnwxMjA3fDB8MHxzZWFyY2h8Mnx8Y293fGVufDB8fDB8fA%3D%3D&ixlib=rb-1.2.1&auto=format&fit=crop&w=1000&q=60')">
        </div>
      <% end %>
  </div>
  <br>
  <div class="booking-form">
    <%= simple_form_for([@item, @booking], html: { class: "form-inline" }) do |f| %>
      <div class="d-flex w-100 justify-content-between">
        <h4>Book your spot at the warehouse:</h4>
        <div class="d-flex">
          <%= f.input :start_date, as: :string, required: true, label: false, input_html: {class: "datepicker", "data-multiple": true}, placeholder: 'Select date' %>
          <%= f.submit "Confirm", class: "form-button ml-5" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
