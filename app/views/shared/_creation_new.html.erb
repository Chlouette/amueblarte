<div class="container page-container">
  <div class="m-5 text-center">
    <h1>Creation time!</h1>
    <p>When are you able to come to our warehouse?</p>
  </div>
</div>
<div class="container page-container d-flex text-center">
  <div class="card-booking">
    <h4>You've chosen to work on this beauty!</h4>
    <br>
    <ul>
      <li>Name: <span class="orange-text"><%= @item.name %></span></li>
      <li>Description: <%= @item.description %></li>
    </ul>
    <div class="d-flex justify-content-center">
      <% if @item.photos.attached? %>
        <% @item.photos.each do |photo| %>
          <div class="booking-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('<%= cl_image_path photo.key %>')">
          </div>
        <% end %>
      <% else %>
        <div class="booking-image" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url('https://images.unsplash.com/photo-1564085352725-08da0272627d?ixid=MnwxMjA3fDB8MHxzZWFyY2h8Mnx8Y293fGVufDB8fDB8fA%3D%3D&ixlib=rb-1.2.1&auto=format&fit=crop&w=1000&q=60')">
        </div>
      <% end %>
    </div>
  </div>

  <br>
  <div class="booking-form">
        <%= simple_form_for([@item, @booking]) do |f| %>
          <div>
            <h4 style="margin-top: 0.7rem">Book your spot at the warehouse:</h4>
            <div>
              <%= f.input :start_date, as: :string, required: true, label: false, input_html: {class: "datepicker", "data-multiple": true}, placeholder: 'Select date' %>
              <div class="d-flex justify-content-end"><%= f.submit "Confirm", class: "form-button" %></div>
            </div>
          </div>
        <% end %>
    </div>
</div>
